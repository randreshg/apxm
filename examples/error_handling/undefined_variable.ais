// INVALID: Undefined variable example
// This should produce a compile-time error because we're using
// a variable that was never defined.

agent UndefinedVarDemo {
    flow main {
        // ERROR: 'undefined_input' is not defined anywhere
        rsn "Process this: " + undefined_input -> result
    }
}
