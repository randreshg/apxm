// INVALID: Type mismatch example
// This should produce a compile-time error because we're passing
// a string where a number is expected.

agent TypeMismatchDemo {
    flow main {
        // ERROR: verify expects a boolean-like result, not a raw string
        rsn "hello" -> result
        verify result with "not_a_valid_verification_expression"
    }
}
