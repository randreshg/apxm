// INVALID: Invalid memory space example
// This should produce a compile-time error because we're using
// a memory space that doesn't exist.

agent InvalidMemoryDemo {
    flow main {
        // ERROR: 'invalid_space' is not a valid memory space
        // Valid spaces are: stm, ltm, episodic
        umem invalid_space "key" <- "value"
    }
}
