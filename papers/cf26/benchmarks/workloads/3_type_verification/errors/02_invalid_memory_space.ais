// ERROR TEST: Invalid Memory Space
// Category: Memory Operations
// Expected: "space must be 'stm', 'ltm', or 'episodic'"
//
// Valid spaces are: stm, ltm, episodic
// This catches typos and invalid tier names at compile-time.

agent InvalidMemoryTest {
    flow main {
        rsn "Generate some data" -> data
        // ERROR: 'permanent' is not a valid memory space
        umem permanent "key" <- data
    }
}
