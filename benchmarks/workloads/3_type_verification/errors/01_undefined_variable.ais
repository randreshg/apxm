// ERROR TEST: Undefined Variable
// Category: Parser/Semantic
// Expected: "Undefined variable: undefined_input"
//
// This is caught at compile-time, before any LLM calls.

agent UndefinedVarTest {
    flow main {
        // ERROR: 'undefined_input' is not defined anywhere
        ask("Process this: " + undefined_input) -> result
    }
}
